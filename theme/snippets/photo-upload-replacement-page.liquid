{% comment %}
  Photo Upload - replacement page entry point
  Version: 2026-09-12.1

  The single line the replacement page template needs:

      {% render 'photo-upload-replacement-page' %}

  Production URL: https://luxmagnetco.com/pages/upload-your-photos
  The page reads session/product/variant from the query string and redirects to
  / if any are missing, so it only works from a customer's replacement link.

  Pulls in styles and core for the shared crop modal; it does not use the
  product-page variants, so no `product` is needed.
{% endcomment %}
{% render 'photo-upload-styles' %}
{% render 'photo-upload-core' %}
{% render 'photo-upload-replacement' %}
